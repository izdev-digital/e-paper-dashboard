name: Build Playwright Component

on:
  workflow_call:
    inputs:
      version:
        description: Component and compatible application version
        required: true
        type: string
  workflow_dispatch:
    inputs:
      version:
        description: Component and compatible application version
        required: true
        type: string

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        include:
          - platform: linux/amd64
            runtime: linux-x64
          - platform: linux/arm64
            runtime: linux-arm64
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
      - uses: docker/setup-qemu-action@v3
      - uses: docker/setup-buildx-action@v3

      - name: Build component payload
        uses: docker/build-push-action@v6
        with:
          context: ./software
          file: ./software/EPaperDashboard.PlaywrightComponent/Dockerfile
          target: export
          platforms: ${{ matrix.platform }}
          build-args: VERSION=${{ inputs.version }}
          outputs: type=local,dest=./component
          cache-from: type=gha,scope=playwright-${{ matrix.runtime }}
          cache-to: type=gha,mode=max,scope=playwright-${{ matrix.runtime }}

      - name: Create archive and checksum
        run: |
          archive="playwright-component-${{ matrix.runtime }}.tar.gz"
          tar --dereference --hard-dereference -C component -czf "$archive" .
          sha256sum "$archive" > "$archive.sha256"

      - uses: actions/upload-artifact@v4
        with:
          name: playwright-component-${{ matrix.runtime }}
          path: |
            playwright-component-${{ matrix.runtime }}.tar.gz
            playwright-component-${{ matrix.runtime }}.tar.gz.sha256
          if-no-files-found: error
